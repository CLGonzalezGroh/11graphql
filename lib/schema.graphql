type Course {
  _id: ID!
  title: String!
  teacher: String
  description: String!
  topic: String
}

type Query {
  "List courses"
  getCourses: [Course]
  "List course by id"
  getCourse(id: ID!): Course
}
